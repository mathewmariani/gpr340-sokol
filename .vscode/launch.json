{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:3000/build/assignments/${command:cmake.buildTargetName}/${command:cmake.activeConfigurePresetName}/${command:cmake.launchTargetFilename}",
            "preLaunchTask": "StartServer",
        }
    ]
}